# Expected outputs

Golden expected markdown for the committed tiny fixtures lives under
`tests/fixtures/`. This directory is reserved for curated human-readable
examples referenced by docs; do not commit full OmniDocBench predictions.
